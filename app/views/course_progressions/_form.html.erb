<%= form_for(@course_progression) do |f| %>
  <% if @course_progression.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_progression.errors.count, "error") %> prohibited this course_progression from being saved:</h2>

      <ul>
      <% @course_progression.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
