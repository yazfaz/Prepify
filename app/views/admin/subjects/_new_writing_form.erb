<%= form_for [:admin, subject] do |f| %>
  <% if @subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject.errors.count, "error") %> prohibited this subject from being saved:</h2>

      <ul>
      <% @subject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field(:section, :value => "Writing") %>
  </div>
  <div class="large-7 columns field flush-left">
    <%= f.text_field :name %>
  </div>
  <div class="large-5 columns actions">
    <%= f.submit "Create Lesson",class: 'tiny button' %>
  </div>
<% end %>
