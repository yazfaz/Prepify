<%= form_for [:admin, @subject, @page] do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field small-field center-form">
    <%= f.label :sequence_id, "Sequence:" %><br>
    <%= f.number_field :sequence_id %>
  </div> 

  <div class="actions center-buttons">
    <%= f.submit class:'tiny button ' %>
    <%= link_to 'Back', admin_subject_pages_path(@subject), class:'tiny button' %>
  </div>
</div>
<% end %>
