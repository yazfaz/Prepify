<h1>Listing instructions</h1>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @instructions.each do |instruction| %>
      <tr>
        <td><%= instruction.body %></td>
        
        <td><%= link_to 'Show', admin_subject_page_instruction_path(@subject, @page, @instruction)%></td>
        <td><%#= link_to 'Edit', edit_instruction_path(@page, @instruction, instruction) %></td>
        <td><%#= link_to 'Destroy', instruction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New Instruction', new_admin_subject_page_instruction_path  %>
