<h1>Listing subjects</h1>

<table>
  <thead>
    <tr>
      <th>Section</th>
      <th>Name</th>
      
    </tr>
  </thead>

  <tbody>
    <% user = @user.id %>
    <% @subjects.each do |subject| %>
      <tr>
        <td><%= subject.section %></td>
        <td><%= subject.name %></td>
        <td><%= link_to "Start Lesson", start_page(user, subject) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%# link_to 'Back', subjects_path %>

