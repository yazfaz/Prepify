<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sequence_id %><br>
    <%= f.number_field :sequence_id %>
  </div>
  <div class="field">
    <%= f.label :pageable_id %><br>
    <%= f.number_field :pageable_id %>
  </div>
  <div class="field">
    <%= f.label :pageable_type %><br>
    <%= f.text_field :pageable_type %>
  </div>
  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
